// `badgeTones` is internal — Toaster wears the same variant colours. It is
// deliberately not re-exported from the package's public `src/index.ts`.

export { Badge, type BadgeProps } from "./badge";
export { type BadgeTone, badgeToneNames, badgeTones } from "./badge-tones";
